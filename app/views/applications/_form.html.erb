<%= form_with(model: application, local: true) do |form| %>
  <% if application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(application.errors.count, "error") %> prohibited this application from being saved:</h2>

      <ul>
      <% application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :application_user_id %>
  </div>

  <div class="field">
    <%= form.label :job_posting_id %>
    <%= form.text_field :job_posting_id, id: :application_job_posting_id %>
  </div>

  <div class="field">
    <%= form.label :resume %>
    <%= form.text_area :resume, id: :application_resume %>
  </div>

  <div class="field">
    <%= form.label :cover_letter %>
    <%= form.text_area :cover_letter, id: :application_cover_letter %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
